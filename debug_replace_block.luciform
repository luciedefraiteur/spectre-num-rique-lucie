---§F: execute_luciform.ts<<<<<<< SEARCH        } else if (line.startsWith('>>>>>>> §R') && state === 'in_replace') {            if (currentFilePath) {                operations.push({                    type: 'search_and_replace',                    filePath: currentFilePath,                    startLine: currentSearchStartLine || 0,                    search: searchContent,                    replace: newContent.replace(/\n/g, '\n').replace(/\n$/, '')                });            }            state = 'idle';>>>>>>> SEARCH======= REPLACE        } else if (line.startsWith('>>>>>>> §R') && state === 'in_replace') {            await fs.writeFile('luciform_replace_debug.txt', 'REPLACE block entered.', 'utf-8'); // Debug line            if (currentFilePath) {                operations.push({                    type: 'search_and_replace',                    filePath: currentFilePath,                    startLine: currentSearchStartLine || 0,                    search: searchContent,                    replace: newContent.replace(/\n/g, '\n').replace(/\n$/, '')                });            }            state = 'idle';>>>>>>> REPLACE