import { LLMInterface, LLMModel } from './luciform-core/llm_interface.js';async function runTest() {    const prompt = "Bonjour, je suis ton instructeur en luciform.";    console.log(`Envoi du prompt au LLM : "${prompt}"`);    try {        const response = await LLMInterface.query(prompt, LLMModel.Mistral);        console.log("Réponse du LLM :\n" + response);    } catch (error) {        console.error("Erreur lors de l'appel au LLM :", error);    }}runTest();