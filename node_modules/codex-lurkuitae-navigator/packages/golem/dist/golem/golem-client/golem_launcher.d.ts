import { LLMModel } from 'luciform-core';
export declare function launchGolem(persona: string, isInteractive: boolean, clientCommands: string[], testInputs?: string[], model?: LLMModel): Promise<void>;
